version: '3.8'

services:
  backend:
    build: ./backend
    container_name: buildmywebsiteai_backend
    restart: always
    environment:
      - DATABASE_URL=mysql+pymysql://avnadmin:AVNS_26Qh9hSxitMP1Yp6Dwt@mysql-278572f1-nervestackers-0979.h.aivencloud.com:15060/defaultdb
      - SECRET_KEY=prod_super_secret_jwt_key_buildmywebsiteai_2026
      - ALGORITHM=HS256
      - ACCESS_TOKEN_EXPIRE_MINUTES=43200
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: buildmywebsiteai_frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend
